#include <Timezone.h>

const char* ssid = "myWifiSSID";   // insert your own ssid 
const char* password = "myWifiPassword";              // and password

#define NTP_ADDRESS  "de.pool.ntp.org"  // change this to whatever pool is closest (see ntp.org/ https://www.pool.ntp.org/de/zone)

const char * days[] = {"Sonntag", "Montag", "Dienstag", "Mittwoch", "Donerstag", "Freitag", "Samstag"} ;

//Timezone
//Central European Time (Frankfurt, Paris, Berlin)
TimeChangeRule CEST = { "CEST", Last, Sun, Mar, 2, 120 };     //Central European Summer Time
TimeChangeRule CET = { "CET ", Last, Sun, Oct, 3, 60 };       //Central European Standard Time
